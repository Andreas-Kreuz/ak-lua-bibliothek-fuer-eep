export default class SocketEvent {
  static JOIN_ROOM = '[join-room]';
  static LEAVE_ROOM = '[leave-room]';

  static ChangeDir = '[Change Dir]';
  static DirOk = '[Dir Ok]';
  static DirError = '[Dir Error]';
}
