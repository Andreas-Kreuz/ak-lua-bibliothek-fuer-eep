export class Switch {
  id: number;
  position: number;
  modelId: number;

  constructor(id: number, position: number, modelId: number) {
    this.id = id;
    this.position = position;
    this.modelId = modelId;
  }
}
